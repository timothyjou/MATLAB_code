-Modded the Rip_hop_test_mode.m file to allow multiple 3dlp to be cropped and print Hopkins output(mean, 25%, 50%, 75%) to .csv in the same folder as the original dlp. 

-Export Ripley plot as .jpeg and .mat to the same folder as the original dlp.

-Rip_hop_modification: allow multiple inputs and export HOP analysis(mean, 25%, 50%, 75% quantile) as .csv file and export RIP plot as .jpeg and .mat file.