-Enable the user to select 1 .3dlp file + multiple .box files(coordinates) in order to automate the cropping from .3dlp file.

**note that the visboxmod.m should be run first to convert the .box to .txt with correct 3rd and 4th column