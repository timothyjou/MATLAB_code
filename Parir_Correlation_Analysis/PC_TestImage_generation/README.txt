This script is used to generate a image from the original .tiff image that would yield a correlation of ~0.5, -1, or 0 when compared to the original image 

** note that output is named as "Test_originalFileName" in the same folder as the original file